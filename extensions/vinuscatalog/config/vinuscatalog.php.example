<?php

return [
    // Install bundled, verified player bridges after successful Minecraft installations.
    'players_auto_install' => true,
    // Private server-side key: never passed to the browser.
    'curseforge_key' => env('VINUS_CURSEFORGE_API_KEY'),
    // Map the egg UUID (not the server UUID) to a Detection::PROFILES key.
    // 'egg_profiles' => ['your-egg-uuid' => 'fabric'],
    'egg_profiles' => [],
    // Optional per-server override, useful when a generic egg launches a different JAR.
    'server_profiles' => [],
];
